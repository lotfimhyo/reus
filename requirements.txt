# Project: Reus
# Founder: Lotfi Mahiddine
# Organization: Reulink
# Contact: Contact@reulink.app

alembic>=1.13
anthropic>=0.40
boto3>=1.43.77
cryptography>=42.0
faiss-cpu>=1.8
fastapi>=0.110
google-genai>=0.5
httpx>=0.27
hvac>=2.0
numpy>=1.26
openai>=1.50
pgvector>=0.3
psutil>=5.9
psycopg[binary,pool]>=3.1
pydantic-settings>=2.2
pydantic>=2.6
redis>=5.0
sqlalchemy>=2.0
uvicorn>=0.29
# فك حزمة uvicorn[standard] يدويًا مع مراقبة صريحة للمنصة، بدل الاعتماد على
# pip-compile ليحافظ على علامة uvloop الشرطية (Unix فقط) تلقائيًا — خلل
# حقيقي أُعيد إنتاجه ثلاث مرات: تشغيل pip-compile عاديًا على هذا النظام
# (لينكس) يُسقِط علامة "extra == 'standard'" الشرطية على uvloop أثناء حل
# التبعيات المتعدية، وينتج قفلًا يُثبِّت uvloop بلا شرط — يفشل فورًا على
# ويندوز (uvloop غير مدعوم إطلاقًا هناك). كتابة الشرط هنا مباشرة، على
# متطلب لا يحتاج pip-compile نفسه أن "يحلّه"، يجعله يُحفَظ بأمانة في القفل.
httptools>=0.6
python-dotenv>=0.13
pyyaml>=5.1
uvloop>=0.15.1; sys_platform != "win32" and sys_platform != "cygwin" and platform_python_implementation != "PyPy"
watchfiles>=0.20
websockets>=13.0
